require 'spec_helper'

describe FlattenRoutes do
  it 'has a version number' do
    expect(FlattenRoutes::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
